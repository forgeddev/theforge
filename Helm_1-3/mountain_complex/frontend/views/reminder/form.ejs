<!DOCTYPE html>
<html>
  <head>
    <% include ../layout/head %>
  </head>
  <body>
    <% include ../layout/header %>

    <main>
      <div class="row">

        <div class="col s12">
          <div class="card horizontal">
            <div class="card-stacked">
              <div class="card-content">

                <h4>Reminders</h4>
                <span>
                  <a href="/reminder/cards"><i class="fg-light-blue small material-icons">skip_previous</i></a>
                  <a id="reminderMetaDataToggle" href="#"><i class="icon-go small material-icons">info_outline</i></a>
                </span>


                <form autocomplete="off" id="reminderForm" class="" action="/reminder/save" method="post">

                  <fieldset class="meta-data" style="display: none;">
                    <legend>Meta Data</legend>
                    <div class="row">
                      <div class="input-field col s12 m4">
                        <input id="recordId" name="recordId" type="text" readonly>
                        <label for="recordId">Record ID</label>
                      </div>
                      <div class="input-field col s12 m4">
                        <input id="parentId" name="parentId" type="text" readonly>
                        <label for="parentId">Parent ID</label>
                      </div>
                      <div class="input-field col s12 m4">
                        <input id="isDeleted" name="isDeleted" type="text" readonly>
                        <label for="isDeleted">Is Deleted</label>
                      </div>
                    </div>
                    <div class="row">
                      <div class="input-field col s12 m6">
                        <input id="createdOn" name="createdOn" type="text" readonly>
                        <label for="createdOn">Created On</label>
                      </div>
                      <div class="input-field col s12 m6">
                        <input id="updatedOn" name="updatedOn" type="text" readonly>
                        <label for="updatedOn">Updated On</label>
                      </div>
                    </div>
                  </fieldset>

                  <div class="row">
                    <div class="input-field col s12">
                      <%- include('../layout/dropdown', {
                        "id": "reminderType"
                        , "label": "Reminder Type"
                        , "category": "Reminder"
                        , "group": "reminderType"
                      });
                      %>
                    </div>
                  </div>

                  <div class="row">
                    <div class="input-field col s6">
                      <input id="when" name="when" type="date" class="validate">
                      <label class="active" for="when">When</label>
                    </div>
                    <div class="input-field col s6">
                      <input id="whenTime" name="whenTime" type="Time" class="validate">
                      <label class="active" for="whenTime">Time</label>
                    </div>
                  </div>

                  <div class="row">
                    <div class="input-field col s12">
                      <textarea id="summary" name="summary" class="materialize-textarea"></textarea>
                      <label for="summary">Summary</label>
                    </div>
                  </div>

                  <div class="row">
                    <div class="input-field col s12">
                      <textarea id="notes" name="notes" class="materialize-textarea"></textarea>
                      <label for="notes">Notes</label>
                    </div>
                  </div>


              </div>
              <div class="card-action">
                <button class="btn waves-effect waves-light" type="submit">Save</button>
                <a class="btn waves-effect waves-light" href="/reminder/cards">Back</a>
              </div>
            </form>
          </div>
        </div>
      </div>

    </main>

    <script type="text/javascript">
    $(document).ready(function() {
      var reference = <%-JSON.stringify(reference)%>

      var reminderEntryForm = new MountainComplexEntryForm({
        "model": 'reminder'
      });

      if( reference && reference.model) {
        reminderEntryForm.load( reference.model);
      } else if( reference && reference.parent){
        $("#reminderForm #parentId").val(reference.parent.recordId);
      }

      // Update Drop Downs
      $('select').material_select();
    });


    </script>

    <% include ../layout/footer %>
  </body>
</html>
