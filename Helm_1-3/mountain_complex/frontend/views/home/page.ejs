<!DOCTYPE html>
<html>
  <head>
    <% include ../layout/head %>
  </head>
  <body>
    <% include ../layout/header %>

    <main>

      <div class="row container">
        <div class="col s12 m12">
          <div class="card">
            <div class="card-image">
              <img src="/images/real_helm_640.png">
              <span class="card-title">Welcome</span>
            </div>
            <div class="card-content">
              <p>
                Helm™ (Health Evaluation & Lifestyle Management) is a software application,
                developed to assist in the removal of barriers that can hinder the delivery of quality care.
                Helm™ provides a central resource for patient navigation and care coordination.
                Helm™ is a web based application that can be used with any mobile device, tablet, or desktop connected to the Internet.
              </p>
            </div>
            <div class="card-action">
              <% if( !authority || (authority && authority.user && authority.user.email && authority.user.email == null)) { %>
                <a href="/register">Register</a>
                <a href="/login">Login</a>
              <% } %>

              <% if( authority && authority.disposition && authority.disposition.indexOf('logged_in') > -1) { %>
                <a href="/changePassword">Change Password</a>
                <a href="/logout">Log Out</a>
              <% } %>

              <a href="/about">About Us</a>
            </div>
          </div>
        </div>

        <% if( authority && authority.disposition && authority.disposition.indexOf('logged_in') > -1) { %>
        <%   include ./features %>
        <% } %>

      </div>
    </main>

    <script type="text/javascript">
    $(document).ready(function() {

    });
    </script>

    <% include ../layout/footer %>
  </body>
</html>
